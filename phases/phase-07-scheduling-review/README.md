# Phase 7 — Scheduling and final review

Schedule only lifecycle behavior already understood manually. Begin with a
local dry run, add expiry reporting and failure handling, and require an
explicit disposable marker before any deletion path can run.

Finish by rebuilding the lab from documented prerequisites, validating it,
demonstrating clone isolation and role boundaries, and cleaning up only the
project-owned objects. Update the root README with a concise architecture,
skills demonstrated, safety decisions, and sanitized evidence suitable for a
portfolio reader.

Checkpoint: explain the SQL/Python/Terraform/scheduler boundary and narrate the
complete system without relying on hidden private notes.
