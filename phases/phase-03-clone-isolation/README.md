# Phase 3 — Clone and isolation

Create `CAR_PRIVATE_TEAM_A`, change it, and prove `CAR_PROD` remains unchanged. Inspect inventories and grants before and after.

Checkpoint: explain why a clone is not a synchronized replica.
