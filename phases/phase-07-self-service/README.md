# Phase 7 — Self-service request

Validate a YAML sandbox request and produce a safe preview before provisioning through the established Python/Terraform boundary.

Checkpoint: explain one valid request and several deliberate rejections.
