# Phase 1 — First Snowflake objects

## Outcome

Create one X-Small auto-suspending warehouse, `CAR_PROD`, `RAW`, a three-row `VEHICLES` table, and an approved view.

Choose Snowsight UI, small numbered SQL statements, or explicitly requested assisted implementation. In every route we inspect each result and review exact cleanup commands.

Checkpoint: explain stored data versus warehouse compute, and table versus view.
